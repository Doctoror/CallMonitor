package com.dd.callmonitor.data.server

/**
 * This port is registered for AllPeers Network, which is discontinued. Meaning there's a good
 * chance no one else will use it.
 *
 * Picking an unassigned port means risking it will be assigned in future. Thus, the chosen port was
 * registered for AllPeers Network is used, which is discontinued. Meaning there's a good chance
 * that no one else will use it.
 *
 * Source [iana.org](https://www.iana.org/assignments/service-names-port-numbers/service-names-port-numbers.xhtml?search=36001])
 */
internal const val PORT = 36001

internal const val SCHEME = "http"
